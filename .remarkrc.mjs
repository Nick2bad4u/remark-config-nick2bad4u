// @ts-check
/**
 * Local Remark configuration for this package.
 *
 * The source of truth is `preset.mjs`, which is also the published shared
 * config entrypoint.
 */

import remarkConfig from "./preset.mjs";

export default remarkConfig;
